# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
# Anchored: unanchored `yay-friend` also matched the cmd/yay-friend/ directory,
# so a new file added beside main.go was silently untracked.
/yay-friend

# Test binary
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work

# Build artifacts
dist/
build/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Local config files that might contain secrets
config.local.*
*.local.yaml
*.local.json